[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
exclude = ["migrations", "docs", "node_modules", ".venv"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.ruff.lint.per-file-ignores]
"skameika/settings/*.py" = ["F405"]